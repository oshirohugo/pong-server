export const TICK_RATE = Number(process.env.TICK_RATE); // updates per second
export const MAP_WIDTH = Number(process.env.MAP_WIDTH);
export const MAP_HEIGHT = Number(process.env.MAP_HEIGHT);

export const BALL_SPEED_X = Number(process.env.BALL_SPEED_X);
export const BALL_SPEED_Y = Number(process.env.BALL_SPEED_Y);
export const BALL_ACCELERATION = Number(process.env.BALL_ACCELERATION);
export const BALL_SZ = Number(process.env.BALL_SZ);

export const PLAYER_SPEED = Number(process.env.PLAYER_SPEED);
export const PLAYER_HEIGHT = Number(process.env.PLAYER_HEIGHT);
export const PLAYER_WIDTH = Number(process.env.PLAYER_WIDTH);
